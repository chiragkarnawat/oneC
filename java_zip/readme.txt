import the workspace file in eclipse  and run it, as the data.csv is in the workspace.

if import fails then please create a maven project, then copy the contents of pom.xml, then both the .java files and data.csv file to the  and the finally run maven clean and maven build then run the program as java application
